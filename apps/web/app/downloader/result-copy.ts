export const HYPEDDIT_ORIGINAL_COPY =
  "Artist original preserved — WAV becomes tagged FLAC; other formats stay unchanged. For Spotify gates, your synced session authorizes the real follow/save requested by the gate.";

/**
 * Shown before the cookie-sync control, never after it: syncing hands the app a
 * session that performs a real follow/save on the user's Spotify account, so the
 * user has to be told that before they can act on the control that grants it.
 */
export const COOKIE_SYNC_DISCLOSURE =
  "Your synced Spotify session authorizes the real follow/save requested by Hypeddit gates.";
